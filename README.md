# Vector-Similarity-Engine-Foundational-AI-Primitive-
A python program that can compute similarity between vectors. Each vector represents a concepts, object or data point. By comparing vectors, We can find which items are "closest" to a given target.
